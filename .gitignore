.build/
target/
.swiftpm/
.DS_Store
*.profraw
# Keep agent guidance tracked; use this private overlay for machine-local instructions.
.agent-local/
dist/
.omo/
# Byte-compiled output from running scripts/*.py; `py_compile` writes it as a
# side effect of the documentation gate, so it is easy to sweep into a commit.
__pycache__/
*.pyc
