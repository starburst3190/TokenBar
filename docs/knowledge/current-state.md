---
status: active
id: kb-current-state
kind: canonical
scope: repository
read_when: starting work, triaging an issue, or deciding whether an upstream item is urgent
last_verified: 2026-07-23
sources: ["public GitHub main history", "public issue #45", "vendor/README.md", "docs/knowledge/history/README.md", "docs/knowledge/plans/tokscale-alignment.md", "docs/knowledge/plans/codex-historical-pace-v2.md", "docs/knowledge/plans/provider-quota-pace.md"]
---

# Current state

## 文件目的

TokenBar native 已完成從 Tauri 到 SwiftUI 的出貨重寫，現在是維護期。這份文件只保留接手時需要的狀態、優先級與公開追蹤入口；完整歷史放在 [`history/`](history/README.md)，vendor 細節放在 [`vendor/README.md`](../../vendor/README.md)。

## 目錄

- [Maintenance posture](#maintenance-posture)
- [Shipped baseline](#shipped-baseline)
- [Priority order](#priority-order)
- [Tracked work](#tracked-work)
- [Deferred and parked](#deferred-and-parked)
- [Handoff questions](#handoff-questions)

---

## Maintenance posture

維護期的排序是 user-reported correctness 優先，其次是可驗證的資料遺失、stale cache、跨語言契約與 release chain 問題；新 client breadth 或大型 UI 重構沒有自動優先權。公開 issue [#45](https://github.com/Nanako0129/TokenBar/issues/45) 是完整 upstream inventory 與決策面，不是「每一列都必須清掉」的 backlog。

> **接手原則：** 先確認問題是否影響實際使用者的數字或更新路徑，再決定要修、報上游、defer，或明確 parked。不要把 inventory 的數量當成工作量承諾。

## Shipped baseline

| Area | Current evidence |
|---|---|
| Product | Native SwiftUI menu-bar app is the shipping line; the predecessor Tauri repository is archived and remains a legacy migration source |
| Vendor | M26-A merged in PR #90 at `95c819c7`; actual M26-B base is current `origin/main` `43fa8ad6`. Upstream target remains `366ce643` across 111 core commits. M24 PR #86 stays closed unmerged and outside the cache dependency graph. This checkpoint is 79 already / 0 take / 0 adapt / 18 defer / 13 skip / 1 superseded, with active format-2 identity shards, format-1 shards rebuilding cold locally, and the former schema-32 monolith retained only as untouched provenance |
| Correctness | Cost provenance, Jcode correction turns, Pi metadata, Claude workflow transcripts, Copilot hierarchy, hidden-client filtering, and bounded folds have landed in staged releases or main history |
| Release | Stable Sparkle feed, Homebrew cask, legacy update metadata, and landing Pages workflows are maintained as separate delivery surfaces |
| Current repository baseline | Before each task, fetch and resolve the current `origin/main`; this document is not a commit pin |

Setup-token quota fallback is shipped: when profile usage is unavailable, provider rate-limit responses can still present quota to the user.

## Priority order

| Priority | Trigger | First reading |
|---|---|---|
| 1 | User-reported wrong or missing usage, cost, quota, or hidden-client data | [`architecture.md`](architecture.md), [`verification.md`](verification.md) |
| 2 | Regression at Rust -> C ABI -> Swift or cache invalidation seam | [`architecture.md`](architecture.md), [`vendor-tokscale.md`](vendor-tokscale.md) |
| 3 | Upstream correctness item with a narrow local adaptation | [`plans/tokscale-alignment.md`](plans/tokscale-alignment.md), [`vendor/README.md`](../../vendor/README.md) |
| 4 | Release, appcast, cask, or migration failure | [`release.md`](release.md), [`history/release-and-ui-incidents.md`](history/release-and-ui-incidents.md) |
| 5 | Cosmetic or broad product expansion | Requires an explicit product decision; do not infer from issue #45 inventory |

## Tracked work

| Workstream | Status | Public surface |
|---|---|---|
| Provider-wide quota pace | Mac implementation is complete through Stage 6 on the task branch：provider／contract／observed duration、generic v3 history、five provider adapters、typed Swift lifecycle／card-ID selection／Historical-only deficit presentation，以及 Rust serializer-locked cross-port fixture。Stage 7 live smoke揭露ad-hoc build無法穩定存取legacy Keychain ACL；尚未出貨的account-scope installation key已改為hardened Application Support內的exact 32-byte、directory `0700`／file `0600`、atomic且cross-process locked file，舊開發item只忽略。Security regressions、Rust workspace tests／Clippy、Rust→Swift build、Swift selftest、docs gates、storage fresh verifier與重新授權的monitored live smoke已通過；smoke未顯示authorization UI，live storage metadata為directory `0700`／file `0600`／exact 32 bytes。明示 `FIXTURE` 的 deterministic popover 已完成 Historical／Linear／Off 驗收：learningDuration 與 typed unavailable 不產生 projection、learningHistory 只使用灰色 Linear estimate、只有 available historical deficit 帶橘色 pace marker／文案；quota 長條的低餘額黃色維持獨立健康訊號。最終 post-GUI fresh verifier 已回傳 `CONFIRMED`，Windows port／parity維持 pending | [`plans/provider-quota-pace.md`](plans/provider-quota-pace.md)、[`plans/codex-historical-pace-v2.md`](plans/codex-historical-pace-v2.md) |
| Copilot upstream contributions | Merged PR #880 remains equivalent to local M10-E hierarchy/cache behavior and needs no port. Duplicate-span issue #938 and PR #939 merged upstream at `1652852f`; post-merge agent/timing gaps are covered by issue #942 and PR #943. PR #943 head `9c399cc5` is maintainer-ready with current-head Codex clean, all CI green, zero unresolved threads, full upstream gates, and fresh verification. These upstream-only commits are outside the fixed 111-row audit and do not change TokenBar schema 32 or ledger counts | [issue #879](https://github.com/junhoyeo/tokscale/issues/879), [PR #880](https://github.com/junhoyeo/tokscale/pull/880), [issue #938](https://github.com/junhoyeo/tokscale/issues/938), [PR #939](https://github.com/junhoyeo/tokscale/pull/939), [issue #942](https://github.com/junhoyeo/tokscale/issues/942), [PR #943](https://github.com/junhoyeo/tokscale/pull/943) |
| Rolling tokscale alignment | M25 merged as PR #75; Grok Build `turn_completed.usage` in PR #77 advanced the former monolith **31 → 32**. M22 PR #72, PR #74, and M24 PR #86 remain closed unmerged fidelity evidence; M23-H and M23-D replaced the bounded PR #74 scope, while M23-V and Warp remain deferred. M26-A merged in PR #90 at `95c819c7`, moved `ae36db5c` to `ALREADY_VENDORED`, and activated format-1 identity shards. M26-B is based on `43fa8ad6` and now activates format 2: generic related-file paths/existence metadata and Claude cached-parent recovery land while the Devin residual stays excluded, moving `cd07bf78` from `TAKE` to `DEFER`. The exact checkpoint is `79/0/0/18/13/1`; format-1 shards rebuild cold locally, the schema-32 monolith remains unread/unmodified/undeleted, and the next graph is M19-B0 → M19-B1 → D2. Swift/FFI settings wiring for aliases remains a follow-up | [issue #45](https://github.com/Nanako0129/TokenBar/issues/45), [PR #72](https://github.com/Nanako0129/TokenBar/pull/72), [PR #74](https://github.com/Nanako0129/TokenBar/pull/74), [PR #75](https://github.com/Nanako0129/TokenBar/pull/75), [PR #77](https://github.com/Nanako0129/TokenBar/pull/77), [PR #82](https://github.com/Nanako0129/TokenBar/pull/82), [PR #83](https://github.com/Nanako0129/TokenBar/pull/83), [PR #86](https://github.com/Nanako0129/TokenBar/pull/86), [`plans/tokscale-alignment.md`](plans/tokscale-alignment.md), [`vendor/README.md`](../../vendor/README.md) |
| Hermes Windows discovery M23-H | Merged as PR #82 at `1a8ee0c6`: when explicit `HERMES_HOME` is absent, scan native `%LOCALAPPDATA%/hermes` and supplied-home `AppData/Local/hermes`; preserve root/profile isolation, plural DB consumers, physical dedup, WAL, mtime, and pruning behavior. Discovery-only change; schema stays 32. Issue #45 bookkeeping is complete | [PR #82](https://github.com/Nanako0129/TokenBar/pull/82), [`plans/tokscale-alignment.md`](plans/tokscale-alignment.md), [`vendor/README.md`](../../vendor/README.md) |
| Copilot Desktop M23-D | Merged as PR #83 at `f99d9274` from upstream `f6f7eced + 0b454e60`: fixed `~/.copilot/data.db` token source, event model/workspace enrichment, DB agent attribution, input-minus-cache normalization, and AIU-only exclusion. One raw OTEL-session selector runs before pricing and every date/client/report fold across materialized, shipping streaming, and count consumers. DB/WAL/event dependencies drive raw cache fingerprints, latest mtime, topology tokens, and fail-open modified-after pruning. Hermetic fixtures cover date-window authority, cold/warm event and WAL invalidation, suppressor retention, full message projection, and graph/model/monthly/hourly/Agents parity. Parser production drift is +9.2% over the final selected upstream parser after preserving dependency metadata/read failures for fail-open cache/pruning behavior, and schema remains 32. PR #74 is closed unmerged as fidelity evidence; issue #45 bookkeeping is complete | [PR #83](https://github.com/Nanako0129/TokenBar/pull/83), [PR #74](https://github.com/Nanako0129/TokenBar/pull/74), [`plans/tokscale-alignment.md`](plans/tokscale-alignment.md), [`vendor/README.md`](../../vendor/README.md) |
| Warp local reporting M24 | PR #86 is closed unmerged. Current-head Codex found four security/state-machine defects; the first unpushed fix round then failed fresh security verification because process-local source/revocation state could delete another process's valid account cache in the shared singleton `warp-usage-v1.json` during 401 handling or Disconnect. Holding the provider refresh lock across remote I/O violates the approved boundary, while releasing it exposes the ownership conflict. The second consecutive review round triggers the fidelity stop: no runtime code landed, schema remains 32, `63a44d7c` is `DEFER`, and the remote branch is retained as evidence | [PR #86](https://github.com/Nanako0129/TokenBar/pull/86), [`plans/tokscale-alignment.md`](plans/tokscale-alignment.md), [`vendor/README.md`](../../vendor/README.md) |
| Identity-aware shard cache M26-A | Merged in PR #90 at `95c819c7`: selectively adopts `ae36db5c` format 1 at `source-message-cache-v2/<namespace>/shard-XX.bin`: per-parser namespace/version identity, 256 shards, 256 MiB per-shard limit, bounded generic/SQLite sampling, Codex prefix state, concurrent-writer merge, compare-before-delete, `fs_atomic` durability, and the selected WalkDir file-type fast path with file-symlink preservation. Native Claude/Copilot/Jcode/Droid/Kimi/Kiro/Roo/Grok dependencies and post-cache authority remain intact. Legacy schema-32 bytes are never read, migrated, rewritten, or deleted; Warp remains `parse_local: false`. M26-A checkpoint was `79/1/0/17/13/1` | [`plans/tokscale-alignment.md`](plans/tokscale-alignment.md), [`vendor/README.md`](../../vendor/README.md) |
| Generic cache format 2 M26-B | Based on `43fa8ad6`: related fingerprints persist exact paths and `exists`, non-`NotFound` dependency failures fail open to cold parsing, exact related sets prevent warm false hits, and Claude reuses cached parent candidates only after a primary match. Format-1 shards rebuild cold locally; the schema-32 monolith remains unread, unmodified, and undeleted. Copilot Desktop dynamic event discovery/fail-open behavior and all Native authority, dedup, pricing, reports, FFI, and Swift seams remain unchanged. The Devin residual stays excluded, so `cd07bf78` is `DEFER`; current checkpoint is `79/0/0/18/13/1` | [`plans/tokscale-alignment.md`](plans/tokscale-alignment.md), [`vendor/README.md`](../../vendor/README.md) |
| OpenCode v2 M20 | Merged as PR #65 at `1bc2fa76`. The implementation ports upstream PR #920 / `366ce643`: existing `opencode-next.db` discovery feeds v2 `session_message` assistant rows, nested model/provider resolution, strict JSON-role behavior, one v1/v2 accumulator whose payload fingerprint excludes cost while retaining provider-reported cost authority, same-id fingerprint-compatible fork collapse, and distinct embedded-id preservation. A shared post-parser identity keeps same-id SQLite rows with incompatible timestamp/token payloads distinct across materialized, shipping streaming, and count lanes. An overlapping v1 row without an embedded id retains its row fallback as an alternate source key while an embedded v2 id remains primary; all-id-less overlaps keep the v1 key primary. Provider authority expands across connected primary/alias groups before streaming, and legacy JSON matches an exact deferred payload through any alias before source-level suppression, preserving cross-channel fork collapse and provider-reported precedence without dropping incompatible siblings. Monolithic cache schema advanced 29 → 30 because a same-fingerprint hybrid database can have a non-empty schema-29 v1-only entry; the hermetic integration fixture rejects it, rebuilds v1+v2, and verifies warm materialized, shipping streaming, count, model, monthly, hourly, and Agents parity. Codex review, CI, fresh verification, merge, and issue #45 bookkeeping are complete | [PR #65](https://github.com/Nanako0129/TokenBar/pull/65), [upstream PR #920](https://github.com/junhoyeo/tokscale/pull/920), [`plans/tokscale-alignment.md`](plans/tokscale-alignment.md) |
| Selected feature completion | This cycle landed Kimi Code, Junie, OpenCodeReview, Copilot Desktop, Hermes Windows discovery, Sakana/Fugu and routed pricing, reloadable grouping aliases, and M26-A's format-1 shard cache. M26-A merged in PR #90 at `95c819c7`; M26-B is based on `43fa8ad6` and lands generic format-2 metadata plus Claude cached-parent recovery while excluding Devin and Pi. Zcode, Copilot VS Code `chatSessions`, and Warp local reporting remain deferred after closed-unmerged PRs #72, #74, and #86. The active cache is format 2; format-1 shards rebuild cold locally, and the schema-32 monolith remains unread/unmodified/undeleted. The next graph is M19-B0 Native canonicalization, M19-B1 Windows final sync, then D2; no M26-B PR or merge SHA is claimed | [`plans/tokscale-alignment.md`](plans/tokscale-alignment.md), [`vendor/README.md`](../../vendor/README.md) |
| Kiro IDE globalStorage M15-A | PR #63 is merged at `632aa739`; snapshot, successful execution, workspace-session, raw-cache/batch-precedence, scanner, mtime, pruning, count, streaming, and report parity coverage are complete for the macOS globalStorage lane. M15-B keeps this cohort separate rather than extending its suppression identities | [PR #63](https://github.com/Nanako0129/TokenBar/pull/63), [upstream PR #715](https://github.com/junhoyeo/tokscale/pull/715), [PR #752](https://github.com/junhoyeo/tokscale/pull/752), [PR #796](https://github.com/junhoyeo/tokscale/pull/796), [PR #799](https://github.com/junhoyeo/tokscale/pull/799), [PR #814](https://github.com/junhoyeo/tokscale/pull/814) |
| Kiro structured sessions M15-B | Merged as PR #66 at `f5773ea0`. The scanner adds only `~/.kiro/sessions/<workspace>/sess_*/session.json` anchors; structured turns read sibling `messages.jsonl`, estimate context/output, preserve flat fallback, and back-calculate missing prompt anchors from turn end and elapsed time. One related-file helper feeds specialized fingerprinting, both cache lanes, latest mtime, and fail-open pruning. IDE-only model fallback is `auto`; existing CLI/SQLite fallback stays `unknown`, so schema 30 remains valid. Hermetic coverage exercises absent/created/rewritten siblings, warm rebuild, M15-A coexistence, materialized/streaming/count, and model/monthly/hourly/Agents parity; Codex review, CI, fresh verification, merge, and issue #45 bookkeeping are complete | [PR #66](https://github.com/Nanako0129/TokenBar/pull/66), [upstream PR #836](https://github.com/junhoyeo/tokscale/pull/836), [PR #847](https://github.com/junhoyeo/tokscale/pull/847), [`plans/tokscale-alignment.md`](plans/tokscale-alignment.md), [`vendor/README.md`](../../vendor/README.md) |
| Existing-parser correctness M16 | Merged as PR #67 at `aebbc371`. Selected scope is Codex legacy replay boundaries and lenient start metadata, Claude bare-transcript explicit-token behavior, start-anchored duration timestamps across Codex/Claude/Copilot/Jcode, monotonic duplicate merging for Claude/Copilot, provider identity recovery, and current Antigravity aliases. The 9Router bridge remains excluded. Existing-source serialized output advanced monolithic cache schema 30 → 31; hermetic parser fixtures and a same-fingerprint schema-30 Jcode entry prove old completion anchoring differs from the rebuilt start anchor, with cold/warm materialized, streaming, count, model, monthly, hourly, and Agents parity. Fresh verification, Codex review, CI, merge, and issue #45 bookkeeping are complete | [PR #67](https://github.com/Nanako0129/TokenBar/pull/67), [upstream PR #896](https://github.com/junhoyeo/tokscale/pull/896), [PR #892](https://github.com/junhoyeo/tokscale/pull/892), [PR #890](https://github.com/junhoyeo/tokscale/pull/890), [PR #891](https://github.com/junhoyeo/tokscale/pull/891), [PR #887](https://github.com/junhoyeo/tokscale/pull/887), [PR #898](https://github.com/junhoyeo/tokscale/pull/898), [`plans/tokscale-alignment.md`](plans/tokscale-alignment.md), [`vendor/README.md`](../../vendor/README.md) |
| Grok unified precedence M17 | Merged as PR #69 at `d4ff968b`. Non-main `ed798642` adds only exact top-level `logs/unified.jsonl` sources beside primary and configured Grok homes while excluding archives. One session-level selector carries unambiguous legacy model/workspace metadata and suppresses covered legacy rows before every materialized, shipping streaming, count, and report fold. Pidless session model events remain authoritative, explicit process starts expire PID-scoped authority, missing loop indexes count as first loops, later loops retain tokens/timestamps with zero messages through live trace and both sessionization folds, cache-read overlap clamps, and topology-sensitive graph/live-tail invalidation restores legacy fallback after unified removal. Schema and the 111-row counts stay unchanged; focused/full gates, fresh verification, Codex `CLEAN`, CI, merge, and issue #45 bookkeeping are complete | [PR #69](https://github.com/Nanako0129/TokenBar/pull/69), [upstream issue #849](https://github.com/junhoyeo/tokscale/issues/849), [`plans/tokscale-alignment.md`](plans/tokscale-alignment.md), [`vendor/README.md`](../../vendor/README.md) |
| New local sources M21 | Merged as PR #71 at `471a7f239f0270b4ebfaed04894335c506d588d3`. Kimi Code reuses the existing `kimi` identity and selects its parser from the `agents/<agent>/wire.jsonl` topology; default and `KIMI_CODE_HOME` roots coexist with legacy Kimi while self-contained Code files do not inherit the legacy `config.json` dependency. Junie and OpenCodeReview append client IDs 31/32, preserve provider-reported Junie cost, start-anchor duration records, carry OpenCodeReview workspace metadata, and flow through materialized, shipping streaming, count, model, monthly, hourly, and Agents paths with raw source caching and per-client dedup. Schema stays 31; M21 moved five audited rows to `ALREADY_VENDORED`, landing at `74/13/0/10/13/1`. The later M22 fidelity decision reclassified five Zcode-bearing rows without changing runtime code, producing `74/8/0/15/13/1`; PR #72 is closed unmerged, so its Zcode code is not present in main | [`plans/tokscale-alignment.md`](plans/tokscale-alignment.md), [`vendor/README.md`](../../vendor/README.md) |
| Reloadable model aliases M25 | Merged as PR #75. Upstream `9a5aeb65` (#850) adds reloadable grouping-only `modelAliases` via `set_model_aliases` / `clear_model_aliases`, an alias-free `canonical_model_id`, and process-wide `model_alias_generation` + invalidation hooks for usage-data consumers. M25 kept the then-active schema 31 and produced the post-M25-only ledger `75/7/0/15/13/1`; current main is schema 32 after PR #77. No FFI/Swift surface landed in this milestone | [`plans/tokscale-alignment.md`](plans/tokscale-alignment.md), [`vendor/README.md`](../../vendor/README.md) |
| Routed pricing M18 | Merged as PR #70 at `0735fd2b`. Audited `959cce84` and `6c804711` add Sakana `fugu-ultra` rates plus the complete routed-pricing precedence pipeline; non-main `548dc124` supplies verified whole-request long-context semantics and `6ea27ca1` supplies bounded prefix/suffix composition. Only verified Sakana and LiteLLM GPT-5.4/GPT-5.5 identities switch tiers when `input + cache_read > 272,000`; exact 272,000 remains regular, output/reasoning follows the request tier, cache-write is excluded, and bare `fugu` remains unpriced. Exact raw/custom and parenthesized keys retain first refusal, provider-scoped paths fail closed, every fallback passes the Claude never-degrade guard, and forced-source isolation, provider ranking, and cache-rate backfill remain intact. Verifier-found provider-scoped custom-normalization and mixed-case forced-source bypasses are fixed by separating internal exact custom lookup from the public normalized API and canonicalizing source selectors once at the shared lookup boundary. GitHub review additionally closed provider-prefixed whole-request tiering, unknown-router Cursor precedence (including suffix-normalized routes), and explicit non-Sakana Fugu attribution gaps, including nested model paths, composite provider hints whose tags disagree, and explicit unknown/blank hints that must not collapse into an unhinted Sakana lookup or cache entry. Pricing remains post-cache retrieval, so schema stays 31; fresh verification, Codex `CLEAN`, CI, merge, and issue #45 bookkeeping are complete at `69/18/0/10/13/1` | [PR #70](https://github.com/Nanako0129/TokenBar/pull/70), [`plans/tokscale-alignment.md`](plans/tokscale-alignment.md), [`vendor/README.md`](../../vendor/README.md) |
| Windows atomic replacement M19-A | Merged as PR #68 at `11ae1bed`. Only `a87f0ab6`'s `fs_atomic.rs` Windows hunk is selected: errors 5 and 32 retry for at most five attempts with exact 10/20/30/40 ms backoff; non-transient errors and non-Windows rename behavior remain single-attempt. Cache schema remains 31; upstream TUI signal behavior is excluded; Codex review, CI, fresh verification, merge, and issue #45 bookkeeping are complete | [PR #68](https://github.com/Nanako0129/TokenBar/pull/68), [upstream PR #906](https://github.com/junhoyeo/tokscale/pull/906), [`plans/tokscale-alignment.md`](plans/tokscale-alignment.md), [`vendor/README.md`](../../vendor/README.md) |
| Zcode M22 | PR #72 is closed with `merged=false`; its 17-commit, 12-file implementation is not in main. Production drift is about `2.1–2.5x` upstream and total drift about `3.1–3.5x`, including about 520 lines of custom cross-store matching and 15 review-driven fixes. DEFER until upstream converges; cache schema remains 31 and no runtime rollback applies | [PR #72](https://github.com/Nanako0129/TokenBar/pull/72), [`plans/tokscale-alignment.md`](plans/tokscale-alignment.md), [`vendor/README.md`](../../vendor/README.md) |
| Day-bar empty-today behavior | Parked, because changing the right edge changes the visible chart and needs a focused fixture plus UI verification | No public commitment beyond the maintenance note |
| Liquid Glass parity | Parked; current glass recipe remains the shipped status quo | [`history/liquid-glass-experiments.md`](history/liquid-glass-experiments.md) |

## Deferred and parked

The following remain outside the selected tokscale cycle: M22 Zcode is deferred because PR #72 closed unmerged and exceeded the fidelity threshold; M23-V Copilot VS Code `chatSessions` is deferred because PR #74 still mis-replays ObjectMutationLog and expanded beyond a bounded source port; M24 Warp local reporting is deferred because PR #86 exposed an unresolved process-local state versus cross-process singleton-cache ownership conflict. M26-A merged independently without Warp; M26-B lands only generic format-2 metadata and Claude cached-parent recovery, while the Devin residual remains deferred. The next ordered graph is M19-B0 → M19-B1 → D2. Command Code, CodeBuddy/WorkBuddy, Devin CLI/Desktop, and 9Router are also deferred. Sakana subscription billing-console scraping is skipped even though Fugu model pricing is selected. A full transparent-panel Liquid Glass re-architecture, project-private maintenance work, user-specific writing or tool preferences, and other-project plans also remain outside this repository queue. Private or other-project material stays classified in [`migration-ledger.md`](migration-ledger.md).

## Handoff questions

Before starting a new task, answer these questions from canonical sources:

| Question | Source |
|---|---|
| Where is the value computed, and is it already pre-aggregated? | [`architecture.md`](architecture.md) |
| What is the smallest deterministic fixture? | [`verification.md`](verification.md) |
| Could a vendor sync erase a local seam? | [`vendor-tokscale.md`](vendor-tokscale.md) and [`vendor/README.md`](../../vendor/README.md) |
| Is the proposed change authorized to reach main or a release channel? | [`workflow.md`](workflow.md) |
| Is this a must-fix, tracked follow-up, or inventory-only item? | This document plus public issue #45 |
